class sereth.sync
  constructor: (spec, source) ->


  # Load single from server/param
  populate: (data = null) ->

  # Load list from server/param
  populate_list: (data = null) ->

  # Update local value
  update: (attr, value) ->

  # Push data to server
  push: ->

  # Get data from server
  pull: ->